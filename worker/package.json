{
  "name": "wzry-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend for 王者测试 — visit tracking + admin dashboard API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute wzry-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute wzry-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
